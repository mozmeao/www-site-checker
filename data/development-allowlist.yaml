# Simple allowlist with hardly any rules
relevant_hostnames:
  - localhost:8000
  - localhost:8080

allowed_outbound_url_regexes:
  # === Broad, trusted cases ===
  - '^(\/|#).*' # Relative URLs and in-page anchors are OK
allowed_outbound_url_literals:
  - "https://other-example.com/two"
